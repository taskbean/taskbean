{
  "name": "taskbean-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:smoke": "npx playwright test --project=smoke"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
