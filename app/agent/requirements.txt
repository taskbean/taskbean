# ── Hardware monitoring ───────────────────────────────────────────────────────
# psutil powers live CPU%/RAM in the status bar (app/agent/main.py
# _hardware_snapshot) and the cold-start RAM-total fallback in
# hardware.py _detect_ram. Pinned explicitly because it's load-bearing
# for the entire status-bar telemetry path; was previously transitive.
psutil>=6.0,<8.0
# pywin32 powers in-process WMI (Win32_VideoController name) and PDH
# (NPU utilization counters) on Windows 11 24H2+. Pinned >=311 because:
#   - 309 and 310 had a DLL-load regression on Win 11 24H2 reported in
#     mhammond/pywin32#2597 (original reporter on 310, build 26100.3775).
#   - 311+ resolves it: empirically verified import of win32api/win32com/
#     win32pdh on Win 11 24H2 build 26220+; zero new reports of #2597-style
#     failures filed against 311 or 312.
#   - Stay open-ended (not pinned ==308) so security fixes flow in. If a
#     future regression shows up, narrow the upper bound here.
pywin32>=311

# Windows ML ExecutionProviderCatalog — vendor-agnostic NPU detection
# on Win 11 24H2+. Requires both the catalog API and the dynamic-
# dependency bootstrap so unpackaged Python processes can call into the
# Windows App SDK without the MSI framework prerequisite. Reference
# Python sample: https://learn.microsoft.com/en-us/samples/microsoft/windowsappsdk-samples/windowsmlpython/
# AMD's Ryzen AI Python sample uses the same pattern:
# https://ryzenai.docs.amd.com/en/latest/winml/winml_ep.html
# Pinned to matching 1.8.x WASDK versions — both packages must come from
# the same WASDK release for ABI compatibility between the catalog API
# and the bootstrap runtime.
wasdk-Microsoft.Windows.AI.MachineLearning==1.8.260317003
wasdk-Microsoft.Windows.ApplicationModel.DynamicDependency.Bootstrap==1.8.260416003

# ── Agent Framework + AG-UI ───────────────────────────────────────────────────
# Versions are pinned because _NormalizingChatClient (in agent.py) overrides
# private framework internals (_inner_get_response, _parse_response_update_from_openai,
# _build_response_stream, _prepare_options) to work around Foundry-Local#422.
# A version bump can silently break the streaming wrapper. Re-verify before unpinning:
# 1. Read agent_framework_openai._chat_completion_client to confirm the method
#    signatures are unchanged.
# 2. Run pytest test_phase0_fixes.py — the wrapper unit tests use real framework
#    types and will fail-fast on shape changes.
# 3. Manual E2E: send a chat through /api/command and watch /api/telemetry/snapshot
#    for ai.response + flow + http.request spans.
agent-framework==1.0.1
agent-framework-openai==1.0.1
agent-framework-ag-ui==1.0.0b260409

# ── Foundry Local (Windows NPU) ───────────────────────────────────────────────
foundry-local-sdk-winml

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart

# ── Token counting ────────────────────────────────────────────────────────────
tiktoken

# ── Scheduling ────────────────────────────────────────────────────────────────
apscheduler

# ── Windows notifications ─────────────────────────────────────────────────────
winotify

# ── MCP (MarkItDown file conversion) ─────────────────────────────────────────
mcp

# ── OpenTelemetry ─────────────────────────────────────────────────────────────
opentelemetry-sdk>=1.29.0
opentelemetry-semantic-conventions>=0.50b0
opentelemetry-exporter-otlp-proto-grpc
opentelemetry-exporter-otlp-proto-http
opentelemetry-instrumentation-fastapi
opentelemetry-instrumentation-httpx

# ── Utilities ─────────────────────────────────────────────────────────────────
httpx
python-dotenv

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest
pytest-asyncio
